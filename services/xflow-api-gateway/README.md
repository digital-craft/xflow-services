# API Gateway

Le service `xflow-api-gateway` est le point d'entrée unique de la plateforme XFlow.

## Rôle
- Routage des requêtes vers les microservices.
- Agrégation de données.
- Sécurité périmétrique (vérification des JWT).

## Configuration
Les routes sont configurées via Spring Cloud Gateway.
